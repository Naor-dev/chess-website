# BFF Pattern: Server-side environment variables (NOT exposed to browser)
# Backend URL for API proxy and auth exchange
BACKEND_URL=http://localhost:3001

# BFF Exchange Secret (MUST match backend BFF_EXCHANGE_SECRET)
# Used to authenticate server-to-server requests to /api/auth/exchange
BFF_EXCHANGE_SECRET=your-secure-random-secret-here

# Google OAuth credentials (same as backend)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Legacy: NEXT_PUBLIC_API_URL is no longer used (BFF proxy handles all API calls)
# NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Sentry Error Tracking (Frontend)
# Client-side DSN (safe to expose - used in browser)
NEXT_PUBLIC_SENTRY_DSN=https://your-public-key@sentry.io/your-project-id

# Server-side Sentry config (for source map uploads)
# These are NOT exposed to browser
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=chess-website-frontend
SENTRY_AUTH_TOKEN=sntrys_your-auth-token-here
